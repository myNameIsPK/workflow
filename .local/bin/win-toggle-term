#!/bin/sh

if [ -z $1 ] || [ -z $2 ]; then
	echo "Usage: $0 <window classname> <command>"
	exit 1
fi

pid=$(xdotool search --class $1)
[ -z $pid ] && $TERMINAL --class $1 -e $2 || xdotool windowkill $pid

#!/bin/sh

flag=$(screenshot -h | sed s/^\\s//g | grep ^- | fzf --nth=1 | awk '{print $1}')

[ -z "$flag" ] && exit 0

filename="screenshot-$(date '+%y%m%d-%H%M-%S').png"
screenshot "$flag" "$filename"

#
# wm independent hotkeys
#
# The valid modifier names are: super, hyper, meta, alt, control, ctrl, shift, mode_switch, lock, mod1, mod2, mod3, mod4, mod5 and any.
# The keysym names are given by the output of xev -event keyboard.

# terminal emulator
super + Return
	$TERMINAL

# program launcher
# super + @space
	# dmenu_run

super + space ; {space, b, e}
  {dmenu_run, $BROWSER, $TERMINAL -e $EDITOR}

# toggle scratchpad
super + d
  [ -z $(xdotool search --class scratchpad) ] && \
  tmux-scratch || \
  win-toggle-hidden scratchpad

# Volume settings
super + v
  win-toggle-term Volume pulsemixer

# Monitoring
ctrl + shift + Escape
  win-toggle-term Monitoring btop

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd
